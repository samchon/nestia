---
title: Tutorial · Welcome
description: "A 30-minute guided tour of Nestia. Build a typed NestJS endpoint, generate a client SDK, get Swagger and e2e tests for free — all from pure TypeScript types."
---

import { Callout } from "nextra/components";

# Welcome to Nestia

Nestia turns a **plain TypeScript type** into everything a NestJS project usually needs to wire by hand — request validators, response serializers, an OpenAPI document, a typed client SDK, and an e2e test suite.

This tutorial is for someone who has never used Nestia before. By the end you will have:

1. A working NestJS server with **type-safe** request validation.
2. A **client SDK** auto-generated from your controllers — you can `npm publish` it.
3. A **Swagger / OpenAPI** document that always matches your code.
4. **E2E tests** generated for every endpoint.
5. A **mockup simulator** so a frontend can call your API before the server exists.

You will write no decorators beyond what you would write in vanilla NestJS, and no separate validation schema.

---

## How long does this take?

About **30 minutes** of reading and copying.

Each page is short and ends with a "Next" link. You can stop after Chapter 5 ("Generate Your SDK") and already have something useful.

---

## What you should know already

- You can read TypeScript.
- You have used NestJS at least once, or you have read its [first chapter](https://docs.nestjs.com).
- You have Node.js 20+ and `npm` (or `pnpm` / `yarn` / `bun`) installed.

That is all. You do not need to know `class-validator`, `class-transformer`, `@nestjs/swagger`, or any code-generation tool.

---

## The 30-second pitch

In vanilla NestJS, a DTO is written three times — once as a TypeScript type, once with `class-validator` decorators, once with `@nestjs/swagger` decorators:

```ts
// vanilla NestJS — 3 places to keep in sync
class CreateArticle {
  @ApiProperty() @IsString() @MinLength(3) @MaxLength(50)
  title!: string;

  @ApiProperty() @IsString()
  body!: string;
}
```

In Nestia, you write the type once. That's it:

```ts
// Nestia — one source of truth
interface ICreateArticle {
  title: string & tags.MinLength<3> & tags.MaxLength<50>;
  body: string;
}
```

Validation, OpenAPI schema, and the SDK type all come from this single declaration. At compile time, a transformer reads the type and emits the runtime code, which is why it can be much faster than `class-validator` while staying simpler.

<Callout type="info">
**Just want to play?** Open the [StackBlitz playground](https://stackblitz.com/github/samchon/nestia-start?file=README.md&view=editor) — a fully wired Nestia project runs in your browser, no local setup needed. You can come back to this tutorial after kicking the tires.
</Callout>

---

## Ready?

**Most readers should head to [Quick Start (5 min)](/tutorial/quickstart)** — that gets you a real Nestia project on your machine and is the path the rest of this tutorial assumes.

Two alternatives, depending on your mood:

- **[StackBlitz Playground](https://stackblitz.com/github/samchon/nestia-start?file=README.md&view=editor)** — instant, in-browser, zero install. Good for kicking the tires before committing to a local setup.
